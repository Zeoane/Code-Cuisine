/*
 * DELETED (dead code) - the old option panel with native <select> dropdowns,
 * replaced by pages/preferences. Not referenced by any page.
 *
 * The file itself could not be removed from disk in this session (sandbox
 * unavailable); emptying it has the same effect for the build. Remove the
 * folder recipes/generator-options entirely when convenient.
 */

export {};
